# Iterations

One file per autonomous-loop iteration, `ITERATION-<NNN>.md`, zero-padded to 3 digits.
Created by `ai-optimization/loop/CONTROLLER.md` Step 15, using the template in
`ai-optimization/loop/ITERATION-PROTOCOL.md`. Every iteration gets a file here whether it
was ACCEPTed or REVERTed — this is a complete chronological record, not just a log of
successes.

Empty as of 2026-08-21 — the loop has not run its first iteration yet. See
`ai-optimization/loop/README.md` "Current status" for what's blocking it.

Do not hand-edit a past iteration's file after the fact except to fix a factual error in
the record itself — the log should reflect what was actually decided at the time, not be
rewritten in hindsight.

# Workstream 2 — Image Opportunities (documented only, not implemented)

Per Workstream 2 instructions, no images were added. This file records genuine visual
needs noticed while building the global design system, for a later, deliberate visual
storytelling pass — consistent with `ai-optimization/DESIGN-OPPORTUNITY-MAP.md` (Stage 2),
which this extends rather than replaces.

| Section | Why a visual is needed | Visual type | Priority | Source |
|---|---|---|---|---|
| Blog cards (`BlogGrid.tsx`) | Every card still shows an icon-in-a-box rather than a photo — now visually consistent via the shared `Card` component, but the underlying content gap (Stage 2 finding) is unchanged | Real featured image per post | P2 | Real (author-provided) |
| About page leadership cards | Still generic `UserRound` icon placeholders, no real photos/names — now styled consistently by the new `Card`, but the content gap remains | Real headshots | P2 | Real |
| `/services/[slug]` "What's included" tool cards (18 pages) | Text-only tool descriptions; a small per-platform "how these tools connect" diagram was already recommended in Stage 2 and still applies | Custom diagram (one per platform, not per service) | P3 | Custom |
| Homepage `EcosystemOrbit` / partner logos | Already well-served by existing real logo assets — explicitly **no new image needed** here | — | — | — |

No stock imagery is recommended anywhere. Per `BRAND_GUIDELINES.md`'s Image Strategy,
real assets (photos, diagrams) rank far above generic conceptual imagery, and none of the
above should be filled with a placeholder just to "finish" the section.
